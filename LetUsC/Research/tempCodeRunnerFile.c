#include<stdio.h>
#define getchh() scanf("%c",&ch);

int main()
{
    char ch;
    char str[10];
    printf("Enter the character ");
    // scanf("%c",&ch);
    // ch = getchar();
    // putchar(ch);
    // gets(str);
    // puts(str);
    getchh();
    printf("%c",ch);
    return 0;
}