#include <stdio.h>
int fact(float x);
int main()
{
    int num;
    printf("Enter any number : ");
    scanf("%d", &num);
    return 0;
}
int fact(float x)
{
    int i, fact;
    {
        fact = fact * i;
    }
    return (fact);
}
